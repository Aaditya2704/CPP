#include <bits/stdc++.h>
using namespace std;

int main()
{

    int size;
    cin >> size;
    int a[size];
    for (int i = 0; i < size; i++)
    {
        cin >> a[i];
    }
    int n; // number that sum of pairs you want to find
    cin >> n;
    // LOGIC
    int count = 0;
    for (int i = 0; i < size; i++)
    {
    }
    cout << count;
    return 0;
}